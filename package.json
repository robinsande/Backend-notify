{
  "name": "notify-backend",
  "version": "1.0.0",
  "description": "Backend for the NOTIFY task reminder system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.0",
    "dotenv": "^16.0.0",
    "express": "^4.19.0",
    "mongoose": "^8.6.3",
    "nodemailer": "^7.0.4"
  }
}
